[project]
name = "integrated_qa_system"
version = "0.1.0"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["base*", "mysql_all*", "app*"]